# Login & Register - Architecture Diagram

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      BROWSER CLIENT                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           Register/Login Form (Blade View)             │   │
│  │  register.blade.php / login.blade.php                  │   │
│  │  - Input fields with IDs (nameReg, emailReg, etc)      │   │
│  │  - Form onsubmit="handleRegister/Login(event)"         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │    Form Handler (public/js/auth-form-handler.js)       │   │
│  │  - Client-side validation                              │   │
│  │  - Email format check                                  │   │
│  │  - Password validation                                 │   │
│  │  - API call to /api/register or /api/login             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │        API Request (JSON POST)                         │   │
│  │  /api/register or /api/login                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│              (Network Request to Backend)                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      LARAVEL BACKEND                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           API Routes (routes/api.php)                  │   │
│  │  POST /api/register → AuthController@register          │   │
│  │  POST /api/login    → AuthController@login             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │      AuthController (app/Http/Controllers/Api)         │   │
│  │  - Validate input (email, password, etc)               │   │
│  │  - Check email/phone unique                            │   │
│  │  - Create user or authenticate                         │   │
│  │  - Create Sanctum token                                │   │
│  │  - Get roles & permissions                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           Database (users, roles, etc)                 │   │
│  │  - User model with Spatie permissions                  │   │
│  │  - Role assignment                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │       JSON Response (token + roles + user)             │   │
│  │  {                                                      │   │
│  │    "message": "Success",                               │   │
│  │    "token": "sanctum_token",                           │   │
│  │    "user": {...},                                      │   │
│  │    "roles": ["requester"],                             │   │
│  │    "permissions": [...]                                │   │
│  │  }                                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      BROWSER CLIENT (Continued)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │     TokenManager (public/js/auth-token-manager.js)     │   │
│  │  - setToken(token, user, roles, permissions)           │   │
│  │  - Save to localStorage                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │          localStorage (Browser Storage)                │   │
│  │  auth_token         → "sanctum_token"                  │   │
│  │  auth_user          → User object JSON                 │   │
│  │  auth_roles         → Roles array JSON                 │   │
│  │  auth_permissions   → Permissions array JSON           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │      Get Dashboard URL based on Role                   │   │
│  │  TokenManager.getDashboardUrl()                        │   │
│  │  - master_admin → /dashboard/admin                     │   │
│  │  - helpdesk → /dashboard/helpdesk                      │   │
│  │  - supervisor → /dashboard/supervisor                  │   │
│  │  - technician → /dashboard/technician                  │   │
│  │  - requester → /dashboard/requester                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │         Redirect to Dashboard                          │   │
│  │  window.location.href = dashboardUrl                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow - Register

```
User Input
    │
    ├─> Name: "John Doe"
    ├─> Email: "john@arwana.com"
    ├─> Phone: "081234567890"
    ├─> Department: 1
    ├─> Password: "password123"
    └─> Confirm: "password123"
         │
         ↓
┌──────────────────────────────┐
│  CLIENT-SIDE VALIDATION      │
│  ✓ Email format              │
│  ✓ Phone format (10-15 dig)  │
│  ✓ Password >= 8 chars       │
│  ✓ Password match            │
│  ✓ All required fields       │
└──────────────────────────────┘
         │
         ↓ (If valid)
┌──────────────────────────────┐
│  SEND POST /api/register     │
│  Content-Type: application/  │
│  json                        │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  BACKEND VALIDATION          │
│  ✓ Validate all fields       │
│  ✓ Email unique              │
│  ✓ Phone unique              │
│  ✓ Password hash             │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  CREATE USER                 │
│  - Insert in users table     │
│  - Assign "requester" role   │
│  - Create Sanctum token      │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  SEND JSON RESPONSE (201)    │
│  {                           │
│    token: "abc123...",       │
│    user: {...},              │
│    roles: ["requester"],     │
│    permissions: [...]        │
│  }                           │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  SAVE TOKEN TO STORAGE       │
│  localStorage.setItem(...)   │
│  TokenManager.setToken(...)  │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  REDIRECT TO DASHBOARD       │
│  window.location.href =      │
│  "/dashboard/requester"      │
└──────────────────────────────┘
```

---

## 🔄 Data Flow - Login

```
User Input
    │
    ├─> Login: "john@arwana.com" or "081234567890"
    └─> Password: "password123"
         │
         ↓
┌──────────────────────────────┐
│  CLIENT-SIDE VALIDATION      │
│  ✓ Email format (if email)   │
│  ✓ Password not empty        │
└──────────────────────────────┘
         │
         ↓ (If valid)
┌──────────────────────────────┐
│  SEND POST /api/login        │
│  {                           │
│    login: "email or phone",  │
│    password: "..."           │
│  }                           │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  DETECT LOGIN TYPE           │
│  - Is email? → email field   │
│  - Is phone? → phone field   │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  AUTHENTICATE USER           │
│  auth()->attempt([...])      │
│  ✓ Find user                 │
│  ✓ Verify password           │
└──────────────────────────────┘
         │
         ├─> Auth Failed? → 401 Error
         │
         ↓ (Auth Success)
┌──────────────────────────────┐
│  CREATE SANCTUM TOKEN        │
│  $user->createToken(...)     │
│  Get user roles & permissions│
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  SEND JSON RESPONSE (200)    │
│  {                           │
│    token: "xyz789...",       │
│    user: {...},              │
│    roles: ["technician"],    │
│    permissions: [...]        │
│  }                           │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  SAVE TO LOCALSTORAGE        │
│  auth_token                  │
│  auth_user                   │
│  auth_roles                  │
│  auth_permissions            │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  GET DASHBOARD URL           │
│  roles[0] → "technician"     │
│  → "/dashboard/technician"   │
└──────────────────────────────┘
         │
         ↓
┌──────────────────────────────┐
│  REDIRECT TO DASHBOARD       │
│  window.location.href =      │
│  "/dashboard/technician"     │
└──────────────────────────────┘
```

---

## 🗂️ File Structure

```
Ticketing-System-Arwana/
│
├── public/
│   └── js/
│       ├── auth-token-manager.js      ← Token management
│       └── auth-form-handler.js       ← Form handling
│
├── resources/
│   └── views/
│       └── auth/
│           ├── register.blade.php     ← Register form (updated)
│           └── login.blade.php        ← Login form (updated)
│
├── app/
│   └── Http/
│       └── Controllers/
│           └── Api/
│               └── AuthController.php ← API endpoints
│
├── routes/
│   └── api.php                        ← API routes
│
├── config/
│   └── permission.php                 ← Role/Permission setup
│
└── Documentation/
    ├── LOGIN_REGISTER_DOCUMENTATION.md
    ├── IMPLEMENTATION_SUMMARY.md
    ├── API_TESTING_GUIDE.md
    ├── QUICK_REFERENCE.md
    └── ARCHITECTURE.md (this file)
```

---

## 📦 Component Dependencies

```
Browser Storage (localStorage)
         ↑
         │
    TokenManager (Manages storage)
         ↑
         │
    Form Handler (Calls TokenManager)
         ↑
         │
    API Responses (From backend)
         ↑
         │
    Blade Views (Display forms)
         ↑
         │
    Browser (User interaction)
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────┐
│  LAYER 1: Frontend Validation       │
│  - Email format check               │
│  - Phone format check               │
│  - Password requirements            │
│  (UX improvement, not security)     │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│  LAYER 2: Backend Validation        │
│  - Server-side checks               │
│  - Database constraints             │
│  - Unique email/phone               │
│  (ACTUAL SECURITY)                  │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│  LAYER 3: Password Hashing          │
│  - bcrypt hashing                   │
│  - Never stored in plain text       │
│  - Verified on login                │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│  LAYER 4: Authentication Token      │
│  - Sanctum API tokens               │
│  - Bearer token in requests         │
│  - Server validates on each request │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│  LAYER 5: Authorization             │
│  - Role-based permissions           │
│  - Middleware checks                │
│  - Policy enforcement               │
└─────────────────────────────────────┘
```

---

## 🌊 Request/Response Cycle

```
┌──────────┐
│ User     │
│ Action   │
└────┬─────┘
     │
     ↓
┌───────────────────────────┐
│ JavaScript Event Handler  │ handleLogin/handleRegister()
└───────────────┬───────────┘
                │
                ↓
        ┌──────────────────┐
        │ Validation       │ Check format, length, etc
        └────────┬─────────┘
                 │
                 ↓
        ┌──────────────────┐
        │ Show Loading     │ Button: "Loading..."
        └────────┬─────────┘
                 │
                 ↓
        ┌──────────────────┐
        │ Fetch API Call   │ POST to /api/register or /api/login
        └────────┬─────────┘
                 │ NETWORK
        ┌────────▼──────────┐
        │ Laravel Backend   │
        │ Process Request   │
        └────────┬──────────┘
                 │ NETWORK
        ┌────────▼──────────┐
        │ JSON Response     │ token, user, roles, permissions
        └────────┬──────────┘
                 │
                 ↓
        ┌──────────────────┐
        │ Check Response   │ Status 200/201?
        └────────┬─────────┘
                 │
         ┌───────┴────────┐
         │                │
    YES  ↓                ↓  NO
    ┌────────────┐    ┌─────────────┐
    │ Save Token │    │ Show Error  │
    └────┬───────┘    └─────────────┘
         │
         ↓
    ┌────────────────┐
    │ localStorage   │
    │ .setItem(...)  │
    └────┬───────────┘
         │
         ↓
    ┌──────────────────────┐
    │ Get Dashboard URL    │
    │ from role            │
    └────┬─────────────────┘
         │
         ↓
    ┌──────────────────────┐
    │ window.location.href │
    │ = dashboardUrl       │
    └────┬─────────────────┘
         │
         ↓
    ┌──────────────────────┐
    │ Browser Navigate     │
    │ to Dashboard         │
    └──────────────────────┘
```

---

## 🎯 Success Criteria

```
✅ REGISTER
  ├─ Form submits via JavaScript (not POST form)
  ├─ Data validated on client & server
  ├─ Token saved to localStorage
  ├─ User redirected to /dashboard/requester
  └─ No page refresh

✅ LOGIN
  ├─ Email or phone accepted as login field
  ├─ Password validated
  ├─ Token & roles saved to localStorage
  ├─ User redirected to correct dashboard
  │  ├─ /dashboard/admin (master_admin)
  │  ├─ /dashboard/helpdesk (helpdesk)
  │  ├─ /dashboard/supervisor (supervisor)
  │  ├─ /dashboard/technician (technician)
  │  └─ /dashboard/requester (requester)
  └─ No page refresh

✅ FUNCTIONALITY
  ├─ Password toggle visibility
  ├─ Departments load from API
  ├─ Form shows loading state during submit
  ├─ Error messages display correctly
  ├─ Success messages display correctly
  └─ Prevent double submit

✅ DATA STORAGE
  ├─ Token accessible via TokenManager
  ├─ User data retrievable
  ├─ Roles accessible
  ├─ Permissions accessible
  └─ Data persists on page refresh
```

